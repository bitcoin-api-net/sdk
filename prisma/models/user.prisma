model User {
  id       String  @id @default(uuid(7))
  email    String  @unique
  password String
  isActive Boolean @default(false) @map("is_active")

  apiKeys ApiKey[]
  boosts  Boost[]

  @@map("users")
}
