{
  "name": "lib",
  "version": "0.0.1",
  "description": "Library package for project. Contains shared libs and utilities.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "type-check": "tsc --build --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "dotenv": "^17.3.1",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  }
}
